upstream _php_socket_app {
    server unix:/socket/php-fpm.sock;
}

server {
    # Порт, который будет слушать nginx, и который пишется вторым в директиве ports в файле docker-compose.yml
    listen 80;
    # Индексный файл, т.е. файл который будет автоматически подставляться при указании корня сайта или директории
    index index.php index.html;
    # Если нужны логи ошибок и доступа, то они доступны в контейнере, смотрите README.md и описание, как войти в bash
    error_log /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
    # Корневая директория проекта, которая смотрит на локальную директорию
    root /var/www/public;
    # Максимальный размер тела запроса, по умолчанию 1m, а значение 0 отключает проверку, часто ставят 128m
	client_max_body_size 128m;


    # параметр, который задает тайм-аут для установки соединения с прокси-сервером. По умолчанию тайм-аут запроса в NGINX равен 60 секундам
    proxy_connect_timeout 600;

    # proxy_send_timeout в Nginx определяет, сколько времени сервер будет ждать, пока клиент отправит всё тело запроса
    proxy_send_timeout 600;

    # proxy_read_timeout — параметр в Nginx, который задаёт тайм-аут при обработке (чтении) ответа, полученного от прокси-сервера.
    proxy_read_timeout 600;

    # директива в Nginx, которая определяет, сколько времени сервер будет ждать, пока клиент отправит весь запрос
    send_timeout 600;

    location / {
        add_header 'Access-Control-Allow-Origin' '*'  always;
        add_header 'Access-Control-Max-Age' '3600'  always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
        add_header 'Access-Control-Allow-Headers' '*' always;
        if ($request_method = OPTIONS ) {
              return 200;
        }
        try_files $uri $uri/ /index.php?$args;
    }
    # Если запрашивается php файл, то требуется обработка с помощью php-fpm интерпретатора
    location ~* \.php$ {
        # Убираем уязвимость https://habr.com/ru/post/100961/
        fastcgi_param PHP_VALUE cgi.fix_pathinfo=0;
        # Если нет нужного php файла, то 404 ошибка, так же убирает уязвимость выше
        try_files $fastcgi_script_name =404;
        # Данные в POST запросе и максимальный размер загружаемого файла не могут превышать 32M. По умолчанию 8M/2M.
        fastcgi_param PHP_VALUE "
            post_max_size=32M
            upload_max_filesize=32M
        ";
        # Выделяем значение $fastcgi_path_info
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        # На каком контейнере докера и на каком порту слушаем PHP
        fastcgi_pass _php_socket_app;
        # Индексный файл для, в данном случае, FPM
        fastcgi_index index.php;
        # Подключаем файл с основными параметрами для FASTCGI (смотри в контейнере)
        include fastcgi_params;
        # Полный путь до скрипта
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        # Значение директории введенного URL
        fastcgi_param PATH_INFO $fastcgi_path_info;
        #  задаёт таймаут при чтении ответа FastCGI-сервера
        fastcgi_read_timeout 600;
    }
}

server {

	listen 443 ssl;
    # Индексный файл, т.е. файл который будет автоматически подставляться при указании корня сайта или директории
    index index.php index.html;

	ssl_certificate		/etc/nginx/cert/rtl.crt;
	ssl_certificate_key	/etc/nginx/cert/rtl.key;
	ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
	ssl_ciphers  "HIGH:!RC4:!aNULL:!MD5:!kEDH";
	add_header Strict-Transport-Security 'max-age=604800';
    root /var/www/public;
    # Максимальный размер тела запроса, по умолчанию 1m, а значение 0 отключает проверку, часто ставят 128m
    client_max_body_size 128m;



    # Все существующие файлы отдавать как есть, иначе, передаются в index.php со всеми аргументами6
    location / {
        add_header 'Access-Control-Allow-Origin' '*'  always;
        add_header 'Access-Control-Max-Age' '3600'  always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
        add_header 'Access-Control-Allow-Headers' '*' always;
        try_files $uri $uri/ /index.php?$args;
    }
    # Если запрашивается php файл, то требуется обработка с помощью php-fpm интерпретатора
    location ~* \.php$ {
        # Убираем уязвимость https://habr.com/ru/post/100961/
        fastcgi_param PHP_VALUE cgi.fix_pathinfo=0;
        # Если нет нужного php файла, то 404 ошибка, так же убирает уязвимость выше
        try_files $fastcgi_script_name =404;
        # Данные в POST запросе и максимальный размер загружаемого файла не могут превышать 32M. По умолчанию 8M/2M.
        fastcgi_param PHP_VALUE "
            post_max_size=32M
            upload_max_filesize=32M
        ";
        # Выделяем значение $fastcgi_path_info
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        # На каком контейнере докера и на каком порту слушаем PHP
        fastcgi_pass _php_socket_app;
        # Индексный файл для, в данном случае, FPM
        fastcgi_index index.php;
        # Подключаем файл с основными параметрами для FASTCGI (смотри в контейнере)
        include fastcgi_params;
        # Полный путь до скрипта
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        # Значение директории введенного URL
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }
}

